# Copy to .env and fill in. Used by the integration test suite.
# .env is gitignored — never commit real credentials.
SUPABASE_TEST_URL=https://<ref>.supabase.co
SUPABASE_TEST_KEY=<service-role-or-anon-key>
